# Backend Internship Blog API

This project includes:  
1. **Blog API**: A RESTful API for managing blogs (CRUD operations).  
2. **Pair Finder**: A script to find pairs in an array that sum to a target.

---

## Technologies
- **Node.js**, **Express.js**, **MongoDB**, **Mongoose**.

---

## Pair Finder:
Run node findPair.js to find pairs in an array summing to a target.
